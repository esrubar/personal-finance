{
  "name": "personal-finance-app",
  "private": true,
  "scripts": {
    "dev": "concurrently \"yarn --cwd frontend dev\" \"yarn --cwd backend-node start\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
